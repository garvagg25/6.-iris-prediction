# 6.-iris-prediction

- imported data using sklearn.datasets
- Normalised features of the dataset
- splitted data into training and testing data
- implimented k neighbourclassifier and checked for different values of k
- Evaluated the model on a confusion matrix
- plotted the model reduced to 2 features
